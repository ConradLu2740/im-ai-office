# 迭代 2 候选对比（路线 B · 一页决策材料）

> 更新：2026-08-30 · 基于 HEAD 65c76a3（路线 A 收官后）
> 用法：在「你的决定」一列打勾/写优先级，勾完我按选定项写 Spec 再实施。

## 候选总览

| # | 候选 | 解决什么痛点 | 价值 | 成本 | 依赖 | 你的决定 |
|---|---|---|---|---|---|---|
| B1 | **任务可修改** | 任务建了就锁死：改负责人/改截止/取消都做不到。真实使用中第一个被同事骂的点 | ★★★★★ 立刻影响日常可用性 | 中（1~2 天）：task 表加操作记录 + confirm/reject 扩展为 update 接口 + 看板卡片加编辑入口 + 提醒随新 deadline 重排 | 无，纯现有栈 | ☐ |
| B2 | **会议纪要场景** | 产品路线 Phase 2 原定主入口："群聊即任务"验证通过后的下一个大故事——一段群聊一键成纪要 + 自动摘任务 | ★★★★☆ 战略价值最大，演示效果最强 | 大（3~5 天）：历史拉取 + LLM 长文摘要 pipeline + 纪要页 UI + 纪要→任务的二次确认流 | 建议 B1 先行（摘出的任务要能改） | ☐ |
| B3 | **记忆页手动增删** | 团队术语库只进不出，识别错了没法修 | ★★★☆☆ 精度兜底，用得越久越需要 | 小（半天~1 天）：memory 路由已有，补 UI 增/删/改 + 权限（谁能删） | 无 | ☐ |
| B4 | **历史消息自动挖掘** | 群里历史聊天批量扫一遍，提取术语/称呼/遗漏任务 | ★★★☆☆ 冷启动利器，但一次性价值 | 中（1~2 天）：分批拉历史 + 复用识别 pipeline + 结果人工确认入库 | 可与 B3 合并成一个"记忆"迭代 | ☐ |
| B5 | 远期：移动端 / 多 Agent 协作 | — | 长线 | 大 | 建议本轮不做 | ☐ |

## 已知技术债（不占迭代，但建议顺手排期）

| 项 | 说明 | 时机建议 |
|---|---|---|
| D3 难例调优 | "阿公 明天上午去演出哦"类自发活动判闲聊、"12点56分"时刻解析缺失（你已决定先不动） | 做 B2 时顺带调 prompt（同一条 pipeline） |
| Tauri Windows GBK | 发送路径乱码，目前浏览器模式替代 | 有 Mac/重装需求时再攻 |
| Mac 网关补丁 | msg_gateway 需加 client_msg_id | 下次在 Mac 上同步时 |
| Windows 开机自启 | 后端+网关手动起 | 随时可做，10 分钟 |

## 我的建议

**B1 → B3 → B2（B4 视情况并入 B3）**：
1. B1 先做——它是"别人真的开始用"的前置条件，且 B2 摘出的任务需要可修改才闭环；
2. B3 小而独立，和 B4 同属"记忆"主题，可以合并成一个小迭代；
3. B2 是迭代 2 的压轴故事，等前两项把地基磨平后一次做透。

## 已选定后的下一步

你勾选后我按惯例：写 Spec（范围/优先级/验收标准）→ 逐项实施（每项独立 commit）→ `python scripts/acceptance.py` + pytest 回归。
